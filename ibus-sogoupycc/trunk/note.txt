Library version
	ibus
		ibus 1.2.0.20090927-1

Environment	
	SOUGOUPYCC_REQUEST_SCRIPT
		location of script which accept fullpinyin and return
		result from server.
	USE_DOUBLEPINYIN
		when set, use doublepinyin instead of normal pinyin,
		
Hot keys
	Shift
		toggle chinese / english
	ESC
		cancel current input
	
Known bugs
	Strange characters will occur when try to type non-lowercase
	letters with non-empty request queue
